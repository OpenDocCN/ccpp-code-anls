# `nmap\libnetutil\ApplicationLayerElement.h`

```cpp
/* This code was originally part of the Nping tool.                        */
// 定义了一个宏，用于防止头文件的重复包含
#ifndef APPLICATIONLAYERELEMENT_H
#define APPLICATIONLAYERELEMENT_H  1

// 包含了PacketElement.h头文件
#include "PacketElement.h"

// 定义了一个类ApplicationLayerElement，继承自PacketElement类
class ApplicationLayerElement : public PacketElement {
};

// 结束宏的定义
#endif
```